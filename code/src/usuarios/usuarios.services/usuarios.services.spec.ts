import { UsuariosServices } from './usuarios.services';

describe('UsuariosServices', () => {
  let provider: UsuariosServices;

  describe('', () => {
    it('should be defined', () => {

      
      expect(provider).toBeDefined();
    });
  });
});
